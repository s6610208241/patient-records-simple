# CLAUDE.md — กติกาการทำงานของ Claude Code ในโปรเจกต์นี้

> ไฟล์นี้ใช้แทน `CLAUDE.md` ของโฟลเดอร์แม่ (VS CODE) ทั้งหมดสำหรับโปรเจกต์นี้
> เพราะโปรเจกต์เดิมเป็น Next.js + Supabase แต่โปรเจกต์นี้เป็น **HTML/CSS/JS ล้วน + Google Sheet**
>
> อ่านไฟล์นี้ก่อนเริ่มทำงานทุกครั้ง และอ่าน `SPEC.md` + `PROJECT_STATUS.md` ประกอบเสมอ

---

## 1. โปรเจกต์นี้คืออะไร

ระบบจัดเก็บและค้นหาประวัติผู้ป่วยในเขตเทศบาล (ผู้ป่วยติดเตียง / ผู้ป่วยธรรมดา)
พร้อมปุ่มนำทางจากเทศบาลไปบ้านผู้ป่วย

- ข้อกำหนดทั้งหมด → `SPEC.md` (เป็นความจริงหนึ่งเดียวของขอบเขตงาน)
- สถานะงานปัจจุบัน → `PROJECT_STATUS.md`
- **ห้ามทำงานนอกเหนือจาก SPEC.md** ถ้าเจอสิ่งที่ SPEC ไม่ได้ระบุ ให้ถามก่อน อย่าเดาแล้วทำเลย

---

## 2. เจ้าของโปรเจกต์ (สำคัญมาก)

ผู้สั่งงาน **อ่านโค้ดไม่ออก** แต่ใช้ AI ทำงานได้คล่อง ดังนั้น:

- อธิบายทุกอย่างเป็น **ภาษาไทย** ด้วยภาษาที่คนทั่วไปเข้าใจ
- ศัพท์เทคนิคทุกคำต้องมีวงเล็บอธิบายครั้งแรกที่ใช้ เช่น "Web App (เว็บที่เปิดให้เรียกใช้ผ่านลิงก์)"
- เมื่อทำงานเสร็จ ให้สรุปเป็น 3 ส่วนเสมอ:
  1. **ทำอะไรไปบ้าง** (ภาษาคน ไม่ใช่ชื่อไฟล์อย่างเดียว)
  2. **ผู้ใช้ต้องไปกดอะไรเองบ้าง** (เช่น สร้าง Google Sheet, deploy Apps Script)
  3. **วิธีทดสอบว่าใช้ได้จริง** (เปิดหน้าไหน กดปุ่มอะไร ควรเห็นอะไร)
- ถ้าต้องให้ผู้ใช้ทำอะไรเองในเว็บอื่น (Google Sheet / Apps Script / GitHub Pages) ให้บอกเป็น
  **ขั้นตอน 1-2-3 พร้อมชื่อปุ่มที่ต้องกด**

---

## 3. กฎเหล็ก (ห้ามฝ่าฝืน)

1. **ไม่มี framework และไม่มี build step** — ห้ามเพิ่ม React / Vue / Next.js / npm / bundler
   ทุกไฟล์ต้องเปิดด้วยการดับเบิลคลิกหรือวางบน static hosting แล้วทำงานได้ทันที
2. **ห้าม hardcode ค่าลับใน `Code.gs`** — รหัสชีต (Sheet ID) และค่าลับอื่นให้เก็บใน
   `PropertiesService.getScriptProperties()` เสมอ
3. **ห้าม hardcode ชื่อระบบ / พิกัดเทศบาล** ในหน้าเว็บ — ต้องอ่านจากแท็บ `Settings` ผ่าน `getSettings` เสมอ
4. **ห้ามเก็บรหัสผ่านไว้ในโค้ดหน้าเว็บ** — การตรวจรหัสผ่านทำที่ Apps Script เทียบกับแท็บ `Users` เท่านั้น
   (ยกเว้นบัญชีจำลองในโหมดเดโมที่อยู่ใน `js/api.js` ซึ่งเป็นข้อมูลสมมติสำหรับสาธิต)
5. **การลบข้อมูลผู้ป่วย = Soft Delete** (ใส่ `deleted_at`) ห้ามลบแถวออกจาก Google Sheet จริง
6. **จุดเริ่มต้นการนำทางต้องเป็นพิกัดเทศบาลเสมอ** ห้ามใช้ตำแหน่งปัจจุบันของผู้ใช้
7. **ห้าม log ข้อมูลผู้ป่วย** (ชื่อ ที่อยู่ อาการ) ลง `console.log` หรือ `Logger.log`
8. **ห้ามคิด URL ของ Apps Script ขึ้นมาเองใส่ใน `API_URL`** — แก้ได้เฉพาะเมื่อผู้ใช้ส่ง URL จริงมาให้เท่านั้น
9. **ห้ามเขียนโค้ดที่ลบแถวหรือลบแท็บใน Google Sheet** โดยไม่ถามก่อน
10. **ทำทีละ Phase** ตาม `PROJECT_STATUS.md` ห้ามข้ามไปทำ Phase ถัดไปเอง
11. **อัปเดต `PROJECT_STATUS.md` ทุกครั้งที่จบงาน**

---

## 4. มาตรฐานการเขียนโค้ด

- **HTML5 + CSS + Vanilla JavaScript (ES6)** เท่านั้น
- ตกแต่งด้วย **Tailwind CSS ผ่าน CDN** เป็นหลัก ส่วน `style.css` ใช้เก็บเฉพาะสิ่งที่ Tailwind ทำไม่สะดวก
  (ตัวแปรสี, ปุ่ม, การ์ด, ช่องกรอก, สถานะโหลด)
- โทนสีของระบบ: **navy (กรมท่า) / teal (เขียวน้ำทะเล) / gold (ทอง)** — ค่าสีจริงอยู่ใน `:root` ของ `style.css`
- ตั้งชื่อไฟล์ `kebab-case.html` / `kebab-case.js`
- **ข้อความบนหน้าจอทั้งหมดเป็นภาษาไทย**
- ทุกฟังก์ชันที่คุยกับ Apps Script ต้องมี `try/catch` และคืนข้อความ error ภาษาไทยที่ผู้ใช้อ่านรู้เรื่อง
- **ทุกหน้าต้องมี 3 สถานะ**: กำลังโหลด (loading) / ไม่มีข้อมูล (empty) / ผิดพลาด (error)
- **Mobile-first**: ออกแบบหน้าจอมือถือก่อนเสมอ ปุ่มต้องสูงอย่างน้อย 44px
- ข้อความทุกอย่างที่มาจากฐานข้อมูลต้องผ่าน `escapeHtml()` ก่อนใส่ลงหน้าเว็บ (กัน XSS)

---

## 5. โครงสร้างโฟลเดอร์

```
/
├─ CLAUDE.md            ← ไฟล์นี้
├─ SPEC.md              ← ข้อกำหนดระบบ
├─ PROJECT_STATUS.md    ← สถานะงาน (อัปเดตทุกครั้ง)
├─ index.html           ← รายการ + ค้นหา
├─ login.html           ← เข้าสู่ระบบ
├─ patient-detail.html  ← รายละเอียด + ปุ่มนำทาง
├─ patient-form.html    ← เพิ่ม/แก้ไข (admin)
├─ trash.html           ← ถังขยะ/กู้คืน (admin)
├─ settings.html        ← ตั้งค่าระบบ (admin)
├─ help.html            ← คู่มือใช้งาน
├─ style.css            ← สไตล์เสริมจาก Tailwind
├─ js/
│   ├─ api.js           ← ตัวกลางเรียก Apps Script + ข้อมูลจำลองโหมดเดโม
│   ├─ auth.js          ← ล็อกอิน/ตรวจสิทธิ์/แถบเมนู + ตัวช่วยที่ใช้ร่วมกัน
│   ├─ patients.js      ← หน้ารายการผู้ป่วย + ฟังก์ชันสร้างลิงก์นำทาง
│   └─ patient-form.js  ← ฟอร์มเพิ่ม/แก้ไข + ดึงพิกัดจากลิงก์แผนที่
└─ apps-script/
    └─ Code.gs          ← โค้ดฝั่ง Google Apps Script (คัดลอกไปวางที่ script.google.com)
```

**หมายเหตุสำคัญเรื่อง `apps-script/Code.gs`:** ไฟล์นี้ไม่ได้ทำงานจากโฟลเดอร์นี้
Apps Script deploy ผ่านหน้าเว็บ script.google.com เท่านั้น ไฟล์ในโปรเจกต์เก็บไว้เป็นต้นฉบับ
เวลาแก้ต้องแก้ที่ไฟล์นี้ก่อนเสมอ แล้วค่อยคัดลอกไปวางทับใน Apps Script

---

## 6. โหมดเดโม (สำคัญ)

`js/api.js` มีตัวแปร `API_URL` อยู่บรรทัดบนสุด

- **ถ้า `API_URL` ว่าง** → ระบบทำงานใน "โหมดเดโม" ใช้ข้อมูลจำลองที่เก็บใน `localStorage`
  และแสดงแถบเหลืองเตือนที่ด้านบนทุกหน้า (บัญชีทดสอบ: `admin/1234`, `staff/1234`)
- **ถ้าใส่ URL จริง** → ทุกหน้าเปลี่ยนไปคุยกับ Google Sheet ทันที โดยไม่ต้องแก้ไฟล์อื่นเลย

ห้ามลบส่วนโหมดเดโมออก เพราะใช้สาธิตตอนที่ยังไม่มี Sheet

---

## 7. วิธีทดสอบ

โปรเจกต์นี้ไม่มี `npm run build` ให้ทดสอบด้วยการเปิดเว็บจริง:

```bash
# วิธีที่ 1: ดับเบิลคลิกไฟล์ login.html ได้เลย
# วิธีที่ 2 (แนะนำ): เปิดเซิร์ฟเวอร์เล็กๆ ในโฟลเดอร์นี้
python -m http.server 5500
# แล้วเปิด http://localhost:5500/login.html
```

---

## 8. เช็กลิสต์ก่อนปิดงานแต่ละ Phase

- [ ] เปิดทุกหน้าในเบราว์เซอร์แล้วไม่มี error ใน Console
- [ ] ทดสอบด้วยบัญชี `admin` แล้วทำงานถูกต้อง
- [ ] ทดสอบด้วยบัญชี `staff` แล้ว **มองไม่เห็นปุ่มเพิ่ม/แก้ไข/ลบ และเข้าหน้า admin ไม่ได้จริง**
- [ ] ย่อหน้าจอเป็นขนาดมือถือ (375px) แล้วไม่ล้น ไม่เพี้ยน
- [ ] ไม่มีค่าลับหรือรหัสชีตหลุดในโค้ดฝั่งหน้าเว็บ
- [ ] อัปเดต `PROJECT_STATUS.md` เรียบร้อย
