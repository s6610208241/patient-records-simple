# PROJECT_STATUS.md — สถานะงาน

> โปรเจกต์: ระบบจัดเก็บและค้นหาประวัติผู้ป่วยในเขตเทศบาล (เวอร์ชันเรียบง่าย)
> สถาปัตยกรรม: HTML + CSS + Vanilla JS + Google Sheet + Google Apps Script
> เริ่มนับใหม่ทั้งหมดตั้งแต่ Phase 0 (เวอร์ชัน Next.js + Supabase ถือเป็นโปรเจกต์เก่า เก็บไว้เป็น reference)

---

## สถานะปัจจุบัน

**Phase 0 และ Phase 1 เสร็จสมบูรณ์แล้ว / Phase 2 กำลังทำ**

ระบบเชื่อมต่อ Google Sheet จริงเรียบร้อย และ **เจ้าของโปรเจกต์ทดสอบด้วยตัวเองผ่านครบทุกฟังก์ชัน
ทั้งบนคอมพิวเตอร์และบนมือถือจริง** รวมถึงเรื่องสิทธิ์ของบัญชี `staff` และหน้าตั้งค่า (ดูหัวข้อ "การทดสอบระบบ")

เอกสารตาม FR-09 ครบทั้ง 2 ฉบับแล้ว และใส่โลโก้จริงเรียบร้อย

**งานถัดไป:** เปลี่ยนรหัสผ่านบัญชีเริ่มต้นในแท็บ Users, ใส่พิกัดเทศบาลจริง
และนำเว็บขึ้น GitHub Pages หรือ Vercel (รอคำสั่งจากเจ้าของโปรเจกต์)

---

## แผนงานทั้งหมด

### Phase 0 — โครงสร้างและหน้าหลัก
- [x] สร้างโครงไฟล์ทั้งหมดตาม SPEC ข้อ 6 — 2026-08-20
- [x] `style.css` โทนสี navy / teal / gold + ปุ่มขนาดนิ้วกด 44px — 2026-08-20
- [x] `js/api.js` ตัวกลางเรียก Apps Script (GET/POST) + โหมดเดโม — 2026-08-20
- [x] `js/auth.js` ล็อกอิน/ตรวจสิทธิ์/แถบเมนูใช้ร่วมกันทุกหน้า — 2026-08-20
- [x] `login.html` เข้าสู่ระบบใช้งานได้จริง (FR-01) — 2026-08-20
- [x] `index.html` รายการ + ค้นหาหน่วง 300ms + กรองประเภท (FR-03, FR-07) — 2026-08-20
- [x] `patient-detail.html` รายละเอียด + ปุ่มนำทาง (FR-05, FR-06) — 2026-08-20
- [x] `patient-form.html` + `js/patient-form.js` ฟอร์มพร้อม validation (FR-04) — 2026-08-20
- [x] `trash.html` ถังขยะและกู้คืน (FR-02) — 2026-08-20
- [x] `settings.html` ตั้งค่าชื่อระบบ/พิกัดเทศบาล (FR-08) — 2026-08-20
- [x] `help.html` คู่มือใช้งานย่อในเว็บ — 2026-08-20
- [x] `apps-script/Code.gs` ครบทุก action ตาม SPEC — 2026-08-20

### Phase 1 — เชื่อมต่อ Google Sheet จริง
- [x] ลูกค้าสร้าง Google Sheet + แท็บ Patients / Users / Settings — 2026-08-20
- [x] นำ `apps-script/Code.gs` ไปวางที่ script.google.com + ตั้ง Script Property `SHEET_ID` — 2026-08-20
- [x] รันฟังก์ชัน `setupSheets` หนึ่งครั้ง — 2026-08-20
- [x] Deploy เป็น Web App แล้วนำ URL มาใส่ `API_URL` ใน `js/api.js` — 2026-08-20
- [x] ทดสอบว่าออกจากโหมดเดโมจริง (แถบเหลืองหายไป) และอ่านค่าจากชีตได้ — 2026-08-20
- [x] ทดสอบล็อกอินกับแท็บ Users จริง + ทดสอบเส้นทางเขียนข้อมูลที่ต้องใช้สิทธิ์ admin — 2026-08-20
- [x] ทดสอบเพิ่ม/แก้ไข/ลบ/กู้คืนผู้ป่วย กับข้อมูลจริงในแท็บ Patients — 2026-08-20
- [ ] เปลี่ยนรหัสผ่านบัญชีเริ่มต้นในแท็บ Users (ตอนนี้ยังเป็น `changeme1234`)

### การทดสอบระบบ (ยืนยันด้วยตาโดยเจ้าของโปรเจกต์ 2026-08-20)

> หมายเหตุ: ไฟล์นี้มีแค่ Phase 0-2 ไม่มี Phase 3 หรือ Phase 9 รายการทดสอบมือถือและ
> รายการอื่นที่เคยกล่าวถึงจึงรวบมาไว้ที่หัวข้อนี้ทั้งหมด เพื่อไม่ให้ต้องกระโดดหาหลายที่

- [x] **ทดสอบบนคอมพิวเตอร์ — ผ่านครบทุกฟังก์ชัน** — 2026-08-20
- [x] **ตรวจการแสดงผลบนมือถือจริง — ผ่านครบ** ทั้งการกรอกฟอร์ม ปุ่มนำทาง และการจัดหน้าจอแบบ responsive (ยืดหยุ่นตามขนาดจอ) — 2026-08-20
- [x] **ทดสอบสิทธิ์บัญชี `staff` แบบเต็มรูปแบบ — ผ่าน** ล็อกอินได้ เมนูถูกจำกัดถูกต้อง และพิมพ์ URL เข้าหน้าของ admin ตรงๆ แล้วถูกเด้งกลับจริง — 2026-08-20
- [x] **ทดสอบหน้า `settings.html` — ผ่าน** แก้ชื่อระบบและพิกัดเทศบาลผ่านหน้าเว็บแล้วบันทึกลงแท็บ Settings ได้จริง — 2026-08-20

### Phase 2 — ข้อมูลจริงและการส่งมอบ
- [x] **ใส่โลโก้จริง** — ฝัง `images/logo.png` เป็นไฟล์ static ในทุกหน้า — 2026-08-20
- [x] `design-document.md` — เอกสารออกแบบ ปรับให้ตรงกับโค้ดจริงครบทุกหัวข้อ (SPEC FR-09) — 2026-08-20
- [x] `docs/user-manual.md` — คู่มือใช้งานแยก admin/staff (SPEC FR-09) — 2026-08-20
- [x] `help.html` — คู่มือใช้งานฉบับเต็มในเว็บ แยกหัวข้อตามสิทธิ์ (SPEC FR-09) — 2026-08-20
- [x] ~~นำเข้าข้อมูลผู้ป่วยเดิม~~ — **ไม่ต้องทำ** ลูกค้ายืนยันว่าไม่มีข้อมูลเดิม — 2026-08-20
- [ ] ใส่พิกัดที่ตั้งเทศบาลจริงในแท็บ Settings
- [ ] นำขึ้น GitHub Pages หรือ Vercel — **รอคำสั่งจากเจ้าของโปรเจกต์**
- [ ] (ถ้ามี) ใส่ชื่อระบบทางการ — ไม่ใช่ blocker แก้เองได้ที่หน้า "ตั้งค่า"

---

## บันทึกการทำงาน (Log)

- 2026-08-20 | Phase 0 | สร้างโปรเจกต์ใหม่ทั้งหมดแบบ HTML/CSS/JS ล้วน แยกจากโปรเจกต์ Next.js เดิมโดยสิ้นเชิง | ไฟล์: ทั้งโปรเจกต์
- 2026-08-20 | Phase 0 | ทำหน้า login.html และ index.html ให้ใช้งานได้จริงด้วยข้อมูลจำลอง | ไฟล์: login.html, index.html, js/api.js, js/auth.js, js/patients.js
- 2026-08-20 | Phase 0 | ทำหน้าที่เหลือตาม SPEC ข้อ 6 ครบ (รายละเอียด/ฟอร์ม/ถังขยะ/ตั้งค่า/คู่มือ) | ไฟล์: patient-detail.html, patient-form.html, trash.html, settings.html, help.html, js/patient-form.js
- 2026-08-20 | Phase 0 | เขียน Code.gs ฉบับเต็มรองรับ 9 action ตาม SPEC | ไฟล์: apps-script/Code.gs
- 2026-08-20 | Phase 1 | ใส่ URL ของ Apps Script Web App ลง `API_URL` ระบบออกจากโหมดเดโมและเชื่อมต่อ Google Sheet จริงสำเร็จ | ไฟล์: js/api.js
- 2026-08-20 | Phase 1 | ทดสอบกับชีตจริง: `getSettings` อ่านค่าได้, ล็อกอิน `admin` ผ่านแท็บ Users ได้, `updateSettings` เขียนกลับได้ (เขียนค่าเดิมทับ ข้อมูลไม่เปลี่ยน), หน้ารายชื่อแสดงสถานะ "ไม่มีข้อมูล" ถูกต้องเพราะแท็บ Patients ยังว่าง, Console ไม่มี error
- 2026-08-20 | Phase 1 | **เจ้าของโปรเจกต์ทดสอบเองผ่านครบทุกข้อ**: บนคอมพิวเตอร์ผ่านครบทุกฟังก์ชัน / บนมือถือจริงผ่านครบ (ฟอร์ม ปุ่มนำทาง responsive) / สิทธิ์ `staff` ถูกจำกัดถูกต้องและพิมพ์ URL เข้าหน้า admin ตรงๆ แล้วถูกเด้งกลับจริง / หน้า `settings.html` แก้ชื่อระบบและพิกัดเทศบาลแล้วมีผลจริง
- 2026-08-20 | Phase 2 | เขียนคู่มือใช้งานฉบับเต็มตาม FR-09 แยกหัวข้อสำหรับ admin และ staff | ไฟล์: help.html, docs/user-manual.md
- 2026-08-20 | Phase 2 | ใส่โลโก้จริงแทนไอคอนชั่วคราว ฝังเป็นไฟล์ static ไม่ผ่าน Settings หรือ Apps Script — ทดสอบแล้วขึ้นครบทั้ง 7 หน้า | ไฟล์: images/logo.png, js/auth.js, login.html, style.css
- 2026-08-20 | Phase 2 | ปรับ `design-document.md` ให้ตรงกับโค้ดจริง เพิ่มตารางคำสั่ง 9 action, กลไกล็อกอิน/token, โครงสร้าง 3 แท็บใน Sheet, รายละเอียดเทคนิค (regex ดึงพิกัด, escapeHtml, LockService, ตรวจข้อมูล 2 ชั้น) และตารางผลทดสอบ | ไฟล์: design-document.md
- 2026-08-20 | Phase 0 | ทดสอบจริงในเบราว์เซอร์ผ่านทุกจุด: ล็อกอิน admin/staff, ค้นหา, กรอง, ดูรายละเอียด, ลิงก์นำทาง, ดึงพิกัดจากลิงก์, validation, เพิ่ม, ลบ (soft), ถังขยะ, ตั้งค่า, และเจ้าหน้าที่เข้าหน้า admin ไม่ได้จริง — ไม่มี error ใน Console

---

## บันทึกการตัดสินใจ (Decision Log)

- **2026-08-20 — ใช้ `patient_code` เป็นตัวระบุผู้ป่วย (ID) แทนการเพิ่มคอลัมน์ id**
  เหตุผล: SPEC ข้อ 5 ไม่มีคอลัมน์ id และ `patient_code` ไม่ซ้ำกันอยู่แล้ว การใช้รหัสเดียวทำให้ลิงก์
  `patient-detail.html?id=P-2026-0001` อ่านแล้วเข้าใจทันที ลดความซับซ้อนของชีต

- **2026-08-20 — ทำ "โหมดเดโม" ไว้ใน `js/api.js` แทนการทำไฟล์ mock แยก**
  เหตุผล: ตอนนี้ยังไม่มี Sheet ID ถ้าแยกไฟล์จะต้องแก้หลายจุดตอนสลับไปใช้ของจริง
  แบบนี้แก้ `API_URL` ที่เดียวแล้วทั้งระบบสลับไปใช้ข้อมูลจริงทันที

- **2026-08-20 — เก็บข้อมูลจำลองไว้ใน `localStorage` ไม่ใช่ตัวแปรในหน่วยความจำอย่างเดียว**
  เหตุผล: ถ้าเก็บในตัวแปรเฉยๆ พอเปลี่ยนหน้าข้อมูลจะรีเซ็ต ทำให้ทดสอบการเพิ่ม/แก้/ลบข้ามหน้าไม่ได้

- **2026-08-20 — ส่ง POST ด้วย `Content-Type: text/plain`**
  เหตุผล: Apps Script ไม่รองรับ CORS preflight (การขออนุญาตข้ามโดเมนล่วงหน้าของเบราว์เซอร์)
  ถ้าส่งเป็น `application/json` เบราว์เซอร์จะบล็อก การส่งเป็น text/plain แล้วให้ฝั่ง Apps Script
  แปลง JSON เองคือวิธีมาตรฐานที่ใช้กันทั่วไป

- **2026-08-20 — เก็บ Sheet ID ไว้ใน Script Properties ไม่ใช่ในโค้ด `Code.gs`**
  เหตุผล: `Code.gs` เป็นไฟล์ที่อยู่ในโปรเจกต์และอาจถูกอัปขึ้น GitHub รหัสชีตไม่ควรติดไปด้วย

- **2026-08-20 — ใส่โลโก้จริงเป็นไฟล์ static ที่ `images/logo.png` ไม่เก็บใน Settings หรือผ่าน Apps Script**
  เหตุผล: SPEC ข้อ 5 กำหนดให้แท็บ Settings มีแค่ `system_name`, `municipality_lat`, `municipality_lng`
  ไม่มีคีย์ `logo_url` และโลโก้แทบไม่มีการเปลี่ยน การทำระบบอัปโหลดไฟล์จึงเกินขอบเขต SPEC ไปมาก
  โลโก้จึงเป็นไฟล์ธรรมดาที่ deploy ไปพร้อมเว็บ ส่วน**ชื่อระบบยังอ่านจากแท็บ Settings เหมือนเดิม**
  จึงยังเปลี่ยนได้เองผ่านหน้า "ตั้งค่า" โดยไม่ต้องแก้โค้ด

- **2026-08-20 — โลโก้ในแถบเมนูแก้ที่ `js/auth.js` ที่เดียว ไม่ได้ไปแก้ทีละไฟล์ HTML**
  เหตุผล: แถบเมนูด้านบนของ 6 หน้า (index, patient-detail, patient-form, trash, settings, help)
  ถูกสร้างจากฟังก์ชัน `Auth.renderHeader()` ร่วมกัน แก้ที่เดียวจึงเปลี่ยนครบทุกหน้าและไม่มีโอกาสตกหล่น
  มีเพียง `login.html` ที่มีโลโก้เป็นของตัวเอง เพราะเป็นภาพหลักกลางจอ ไม่ได้อยู่ในแถบเมนู

- **2026-08-20 — ปุ่มนำทางอยู่เฉพาะหน้ารายละเอียดผู้ป่วย ไม่ใส่ในการ์ดหน้ารายการ**
  เหตุผล: SPEC ข้อ 6 ระบุว่าหน้ารายการคือ "รายการ + ค้นหา" ส่วนปุ่มนำทางอยู่ที่หน้ารายละเอียด
  ไม่เพิ่มฟีเจอร์เกินจาก SPEC

---

## สิ่งที่ติดอยู่ (Blockers) — รอข้อมูลจากลูกค้า

- [x] ~~**Google Sheet + Apps Script**~~ — เชื่อมต่อสำเร็จแล้ว 2026-08-20
- [x] ~~**โลโก้**~~ — ได้รับไฟล์จริงและใส่เรียบร้อยแล้ว 2026-08-20
- [x] ~~**ข้อมูลผู้ป่วยเดิมที่ต้องนำเข้า**~~ — ลูกค้ายืนยันว่าไม่มีข้อมูลเดิม 2026-08-20
- [ ] **รหัสผ่านบัญชีเริ่มต้นยังเป็นค่าตั้งต้น** — แท็บ Users ยังเป็น `changeme1234` ทั้ง `admin` และ `staff` ต้องเปลี่ยนก่อนใช้งานจริง
- [ ] **พิกัดที่ตั้งเทศบาล** — ตอนนี้ใช้พิกัดตัวอย่างกลางกรุงเทพฯ ต้องเปลี่ยนก่อนใช้งานจริง
- [ ] **รายชื่อผู้ใช้เริ่มต้น + ใครเป็น admin**
